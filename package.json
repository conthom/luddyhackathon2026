{
  "name": "luddyhackathon2026",
  "private": true,
  "workspaces": [
    "leaderboard-server",
    "leaderboard-client"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w leaderboard-server\" \"npm run dev -w leaderboard-client\"",
    "dev:server": "npm run dev -w leaderboard-server",
    "dev:client": "npm run dev -w leaderboard-client",
    "build": "npm run build -w leaderboard-server && npm run build -w leaderboard-client",
    "start": "npm run start -w leaderboard-server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
